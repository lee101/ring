dependencies:
  pre:
    - npm install -g gulp

deployment:
  prod:
    branch: prod
    commands:
      - bash ./setup-eb.sh
      - gulp build
      - git commit -a -m "ci gulp"
      - eb deploy
machine:
  node:
    version: 0.12.0
